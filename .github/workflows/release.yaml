name: Package and Release Sources

on: [workflow_dispatch]

jobs:
  package:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.x'

    - name: Call zip_sources
      id: zip_sources
      run: |
        PYTHONPATH=src script_output=$(python src/pack_sources.py)
        zip_file=$(echo "$script_output" | sed -n '1p')
        version=$(echo "$script_output" | sed -n '2p')
        echo "zip_file=$zip_file" >> $GITHUB_ENV
        echo "version=$version" >> $GITHUB_ENV

    - name: Create GitHub Release
      id: create_release
      uses: actions/create-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: ${{ env.version }}
        release_name: Release ${{ env.version }}
        draft: false
        prerelease: false

    - name: Upload Release Asset
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }}
        asset_path: ${{ env.zip_file }}
        asset_name: sources.zip
        asset_content_type: application/zip